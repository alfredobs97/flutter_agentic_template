/// Barrel for `failures/`. See `domain_failure.dart` for the full
/// rationale of the sealed failure hierarchy and AGENTS.md → "Error
/// handling" for how `data` and `ui` must use it.
library;

export 'domain_failure.dart';
